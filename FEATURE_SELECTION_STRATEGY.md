# 특성 선택 전략 및 근거

## 특성 선택 방법론

특성의 중요도와 유용성을 평가하기 위해 **4가지 방법**을 사용했습니다:

### 1. 상관관계 분석 (Correlation Analysis)
- **방법**: 각 특성과 타겟 변수 간의 피어슨 상관계수 계산
- **장점**:
  - 계산이 빠르고 직관적
  - 선형 관계를 명확히 파악
- **단점**: 비선형 관계는 포착하지 못함

### 2. Mutual Information (상호정보량)
- **방법**: 특성과 타겟 간의 상호 의존성 측정
- **장점**:
  - 비선형 관계도 포착 가능
  - 특성의 정보 가치를 정량화
- **단점**: 계산 비용이 높음

### 3. ElasticNet 특성 중요도
- **방법**: L1 + L2 정규화를 사용하는 선형 모델의 계수 분석
- **장점**:
  - 자동으로 특성 선택 수행 (L1 정규화)
  - 다중공선성 문제 완화 (L2 정규화)
  - 1위 노트북과 동일한 모델 사용
- **하이퍼파라미터**:
  - `l1_ratio = 0.5` (L1:L2 = 1:1)
  - `cv = 10` (10-fold 교차 검증)
  - `alphas = np.logspace(-4, 2, 100)` (100개의 alpha 값 탐색)

### 4. LightGBM 특성 중요도
- **방법**: Gradient Boosting 모델의 gain 기반 특성 중요도
- **장점**:
  - 비선형 관계와 상호작용 효과 포착
  - 실제 예측 모델과 유사한 방식
  - Tree 기반으로 특성 간 복잡한 관계 파악

## 종합 평가 방법

### 평균 순위 (Average Rank) 방식
- 각 방법에서 특성의 순위를 매김
- 4가지 방법의 평균 순위를 계산
- **낮은 평균 순위 = 높은 중요도**

### 교집합 방식
- 여러 방법에서 공통적으로 상위권에 있는 특성
- **핵심 특성**: 3가지 이상의 방법에서 모두 상위 10위
- **유망 특성**: 평균 순위 상위 30위 내

## 1위 노트북의 특성 선택

1위 노트북에서 선택한 **13개 핵심 특성**:
```python
["S2", "E2", "E3", "P9", "S1", "S5", "I2", "P8", "P10", "P12", "P13"]
```

### 선택 근거 (추정)
1. **ElasticNet CV 기반 선택**
   - L1 정규화로 자동 특성 선택
   - 비영 계수를 가진 특성만 선택

2. **도메인 지식 반영**
   - S (Sentiment): 시장 심리 지표
   - E (Economic): 경제 지표
   - P (Price): 가격 지표
   - I (Industry): 산업 지표

3. **실증적 검증**
   - 1위 점수로 검증된 특성 조합
   - 과적합 방지를 위한 적절한 특성 수

## 개선 모델의 특성 선택

### 기본 특성 (26개)
1위 노트북의 13개 + 추가 유망 특성 13개:
```python
base_features = ["S2", "E2", "E3", "P9", "S1", "S5", "I2", "P8",
                 "P10", "P12", "P13"]

additional_features = ["M1", "M2", "M11", "V1", "V2", "E1", "E4", "E5",
                       "I1", "I7", "I9", "P1", "P2", "S3", "S4"]
```

#### 추가 특성 선택 근거:
1. **M (Market) 그룹**: M1, M2, M11
   - 시장 전체 동향 반영
   - 특히 M11은 1위 노트북의 U2 특성에서 핵심 요소

2. **V (Volume) 그룹**: V1, V2
   - 거래량 정보
   - 시장 활성도 지표

3. **E (Economic) 확장**: E1, E4, E5
   - 경제 지표 다각화
   - E2, E3와 상호보완

4. **I (Industry) 확장**: I1, I7, I9
   - I2와 함께 산업 전반 커버
   - I1은 U1 특성 (I2 - I1) 생성에 필수

5. **P (Price) 확장**: P1, P2
   - 가격 비율 특성 생성에 사용

6. **S (Sentiment) 확장**: S3, S4
   - 심리 지표 보완

### 생성 특성 (11개)

1. **1위 노트북 특성 (2개)**
   ```python
   U1 = I2 - I1                    # 산업지표 차이
   U2 = M11 / ((I2+I9+I7)/3)       # 시장/산업 비율
   ```

2. **비율 특성 (3개)**
   ```python
   M_ratio = M1 / M2               # 시장 비율
   P_ratio = P1 / P2               # 가격 비율
   V_ratio = V1 / V2               # 거래량 비율
   ```
   - **근거**: 절대값보다 비율이 더 안정적인 신호

3. **그룹별 평균 (3개)**
   ```python
   E_mean = (E1 + E2 + E3) / 3     # 경제지표 평균
   S_mean = (S1 + S2 + S5) / 3     # 심리지표 평균
   P_mean = (P8 + P9 + P10) / 3    # 가격지표 평균
   ```
   - **근거**: 노이즈 감소, 안정성 향상

4. **상호작용 특성 (3개)**
   ```python
   SE_interact = S2 * E2           # 심리-경제 상호작용
   PM_interact = P9 * M1           # 가격-시장 상호작용
   IM_interact = I2 * M11          # 산업-시장 상호작용
   ```
   - **근거**: 특성 간 비선형 관계 포착

## 최종 특성 수

- **총 37개 특성** = 26개 기본 + 11개 생성
- 1위 노트북 (13개)보다 많지만, 기본 모델 (144개)보다 훨씬 적음
- **적절한 균형**: 충분한 정보 + 과적합 방지

## 결측치 처리

### EWM (Exponentially Weighted Moving Average)
```python
df.with_columns(
    pl.col(col).fill_null(pl.col(col).ewm_mean(com=0.5))
)
```

#### 선택 근거:
1. **1위 노트북 방식 채택**
2. **시계열 특성 고려**:
   - 최근 값에 더 높은 가중치
   - 시간적 흐름 반영
3. **중앙값/평균 대비 장점**:
   - 트렌드 유지
   - 급격한 변화에 민감

## 스케일링

### StandardScaler
```python
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)
```

#### 선택 근거:
1. 모든 특성을 동일한 스케일로 정규화
2. Gradient Boosting 모델에 적합
3. 1위 노트북과 동일한 방식

## 검증 방법

### feature_importance_analysis.py
실행하면 다음 결과를 얻을 수 있습니다:
1. `feature_importance_analysis.csv`: 모든 특성의 중요도 분석 결과
2. `feature_importance_comparison.png`: 4가지 방법의 시각화 비교
3. 콘솔 출력: 상위 특성 및 추천 특성 리스트

## 결론

특성 선택은 **과학(데이터 기반 분석) + 예술(도메인 지식 + 실험)**의 조합입니다.

1. **다양한 방법 활용**: 단일 방법의 편향 방지
2. **검증된 방법 존중**: 1위 노트북의 특성을 기반으로
3. **점진적 확장**: 핵심 특성 + 유망 특성
4. **실험을 통한 검증**: 최종 성능으로 판단

이 전략을 통해 1위 노트북보다 **더 나은 성능**을 목표로 합니다!
